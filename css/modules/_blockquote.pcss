blockquote {
  border-left: var(--spacing-extra-extra-small) solid var(--eggplant);
  padding: var(--spacing-medium);
  margin: var(--spacing-large) 0;
  background: var(--snow);
}

blockquote p {
  margin: 0;
}

blockquote p + p {
  margin-top: var(--spacing-medium);
}
