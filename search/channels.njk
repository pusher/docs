---
permalink: search/channels.json
---
{%- set posts = collections.channels | reverse -%}
[
{%- for post in posts -%}
  {
    "title": "{{ post.data.title | default(" ") }}",
    "date": "{{ page.date }}",
    "url": "{{ post.url }}",
    "content": "{{ post.templateContent | algExcerpt | jsonify }}"
  }{% if not loop.last %},{% endif %}
{% endfor %}
]
